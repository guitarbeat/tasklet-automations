# Packages

Shared code and utilities for the monorepo. Each subdirectory is a pnpm workspace package.

No packages yet — add one with:

```bash
mkdir -p packages/my-package
cd packages/my-package
pnpm init
```
